spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ticketguard?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: root
    password: "6891"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: your-very-long-secret-key-at-least-32-characters-long-12345
  expiration: 3600000 # 1시간 (밀리초)